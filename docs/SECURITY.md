# 安全说明

- 不要提交 `.env`、`secrets/`、SSH 私钥、GitHub Token 或服务器密码。
- 默认只将管理 API 暴露到 `127.0.0.1:5000`。
- 如需远程访问管理 API，请使用 VPN 或带身份认证的反向代理。
- 建议为发布用户配置最小权限，并限制其只能写博客源码和站点目录。
- GitHub Token 只授予目标图库仓库所需的最小权限。
- 首次发布前检查 Markdown、图片和项目数据中是否含本机绝对路径。
- 定期备份 `data/`，但不要将真实备份提交到公开仓库。